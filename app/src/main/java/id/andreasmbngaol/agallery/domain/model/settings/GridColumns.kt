package id.andreasmbngaol.agallery.domain.model.settings

/** Grid column bounds and default for the gallery (shared across layers). */
const val MIN_GRID_COLUMNS = 3
const val MAX_GRID_COLUMNS = 5
const val DEFAULT_GRID_COLUMNS = 4

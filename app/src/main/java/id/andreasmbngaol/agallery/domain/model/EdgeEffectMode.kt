package id.andreasmbngaol.agallery.domain.model

/**
 * Mode efek di tepi layar (area status bar & navigation bar).
 * FROSTED = blur kaca, GRADIENT = gradasi gelap, OFF = tanpa efek.
 */
enum class EdgeEffectMode {
    FROSTED,
    GRADIENT,
    OFF,
}
